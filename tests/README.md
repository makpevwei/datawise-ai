# Cross-cutting tests

Backend unit tests live in `../backend/tests`. This directory is reserved for tests
that span both backend and frontend (e.g. end-to-end flows) once there is
functionality to exercise end-to-end.
